{
  "name": "deeporigin-cypress-api-ts",
  "version": "1.0.0",
  "private": true,
  "description": "API tests for DummyJSON Products using Cypress + TypeScript",
  "scripts": {
    "test": "cypress run --config-file cypress.config.ts",
    "test:open": "cypress open --config-file cypress.config.ts",
    "lint:types": "tsc --noEmit",
    "ci": "npm ci && npm run test"
  },
  "devDependencies": {
    "cypress": "^13.13.2",
    "typescript": "^5.4.5",
    "@types/node": "^20.11.30"
  }
}